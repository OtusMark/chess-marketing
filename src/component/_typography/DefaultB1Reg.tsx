import styled from "styled-components/macro";

export const DefaultB1Reg = styled.p`
  font-size: ${({theme}) => theme.font.size.b1};
  font-weight: 300;
`

// Default global font style is located in html tag in GlobalStyles!!!